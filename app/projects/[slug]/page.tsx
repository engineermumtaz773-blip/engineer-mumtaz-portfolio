import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, CheckCircle2, ExternalLink, Github } from "lucide-react";
import { projects } from "@/data/projects";
import { absoluteUrl } from "@/lib/utils";
import { Button } from "@/components/ui/button";
import { ContactCta } from "@/components/sections/contact-cta";
import { BreadcrumbJsonLd } from "@/components/seo/json-ld";

export function generateStaticParams() { return projects.map((project) => ({ slug: project.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const project = projects.find((item) => item.slug === slug); if (!project) return {}; return { title: project.title, description: project.description, alternates: { canonical: absoluteUrl(`/projects/${project.slug}`) }, openGraph: { title: project.title, description: project.description, url: absoluteUrl(`/projects/${project.slug}`) } }; }
export default async function ProjectPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params; const project = projects.find((item) => item.slug === slug); if (!project) notFound();
  return <><BreadcrumbJsonLd items={[{ name: "Home", path: "/" }, { name: "Projects", path: "/projects" }, { name: project.title, path: `/projects/${project.slug}` }]} /><section className="bg-navy pb-18 pt-36 text-white sm:pt-40"><div className="container-shell"><Link href="/projects" className="inline-flex items-center gap-2 text-sm font-bold text-slate-400 hover:text-white"><ArrowLeft className="h-4 w-4" /> Back to projects</Link><div className="mt-10 grid gap-10 lg:grid-cols-[1fr_.65fr] lg:items-end"><div><span className="rounded-full bg-white/10 px-4 py-2 text-xs font-bold uppercase tracking-[0.16em] text-cyan-200">{project.category}</span><h1 className="mt-6 font-display text-5xl font-bold leading-[1] tracking-[-0.055em] sm:text-7xl">{project.title}</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-slate-300">{project.description}</p></div><div className="flex flex-wrap gap-3 lg:justify-end"><Button asChild variant="dark"><Link href={project.liveUrl}><ExternalLink className="h-4 w-4" /> Live demo</Link></Button><Button asChild variant="dark"><Link href={project.githubUrl}><Github className="h-4 w-4" /> GitHub</Link></Button></div></div></div></section><section className="bg-surface py-12"><div className="container-shell"><div className="relative aspect-[16/8] overflow-hidden rounded-[30px] border border-slate-200 bg-white shadow-[var(--shadow-card)]"><Image src={project.image} alt={`${project.title} project preview`} fill priority className="object-cover" sizes="100vw" /></div></div></section><section className="section-space bg-white"><div className="container-shell grid gap-12 lg:grid-cols-[1fr_.7fr]"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-brand">Case study framework</p><h2 className="mt-4 font-display text-4xl font-bold tracking-[-0.05em] text-navy">Problem, system, and outcome.</h2><div className="mt-7 space-y-5 leading-8 text-slate-600"><p><strong className="text-navy">Problem:</strong> The project required a credible interface and maintainable technical system capable of supporting core user journeys without unnecessary complexity.</p><p><strong className="text-navy">Approach:</strong> Requirements were organized into reusable components, responsive constraints, clear data structures, validation boundaries, and purposeful interaction patterns.</p><p><strong className="text-navy">Outcome:</strong> The result demonstrates a scalable delivery pattern suitable for production refinement, verified content, real integrations, analytics, and stakeholder testing.</p></div></div><aside className="rounded-[28px] border border-slate-200 bg-surface p-7"><h2 className="font-display text-xl font-bold text-navy">Key features</h2><div className="mt-5 space-y-3">{project.features.map((feature) => <p key={feature} className="flex gap-3 text-sm font-semibold text-slate-700"><CheckCircle2 className="h-5 w-5 shrink-0 text-brand" />{feature}</p>)}</div><h2 className="mt-8 font-display text-xl font-bold text-navy">Technology</h2><div className="mt-4 flex flex-wrap gap-2">{project.technologies.map((item) => <span key={item} className="rounded-full border border-slate-200 bg-white px-3 py-1.5 text-xs font-semibold text-slate-600">{item}</span>)}</div></aside></div></section><ContactCta /></>;
}
